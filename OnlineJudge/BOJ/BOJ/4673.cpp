#include <stdio.h>
int a[10000];
int main()
{
	for(int i= 0; i<10000; i++)
	{
		if((i+1)/10 >= 100)
		{
			a[(i+1) + ((i+1)/1000) + (((i+1)/100)%10) + (((i+1)/10)%10)+ ((i+1)%10)]=1;
		}
		else if((i+1)/10 >= 10)
		{
			a[(i+1) + ((i+1)/100) + (((i+1)/10)%10) + (((i+1))%10)]=1;
		}
		else if((i+1)/10 >= 1)
		{
			a[(i+1) + (i+1)/10 + ((i+1))%10]=1;
		}
		else
		{
			a[(i+1)+(i+1)]=1;
		}
	}
	for(int i= 1; i<10000; i++)
	{
		if(a[i]==0)
			printf("%d\n",i);
	}

}